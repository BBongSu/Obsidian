---
sticker: emoji//270f-fe0f
---

``` js title="getCommaFormatter - 세 자리마다 콤마가 포함된 숫자 형식으로 변환"
function getCommaFormatter(prefixSelector) {
	const input = prefixSelector;
	if(!input) return;
	
	input.on("input",function(){
		let val = this.value.replace(/[^0-9]/g, '');
		if(val === '') return this.value = '';
		this.value = Number(val).toLocaleString();
	});
}
```
``` js title="setCookie - 쿠키 설정"
function setCookie(name, value) {
	var todayDate = new Date();
	todayDate.setDate(todayDate.getDate() + 365);
	document.cookie = name + "=" + escape(value) + "; path=/; expires="
	+ todayDate.toGMTString() + ";"
}
```
``` js title="getCookie - 쿠키 가져오기"
function getCookie(cookie_name) {
	var x, y;
	var val = document.cookie.split(';');

	for (var i = 0; i < val.length; i++) {
		x = val[i].substr(0, val[i].indexOf('='));
		y = val[i].substr(val[i].indexOf('=') + 1);
		x = x.replace(/^\s+|\s+$/g, ''); // 앞과 뒤의 공백 제거하기
		if (x == cookie_name) {
			return unescape(y); // unescape로 디코딩 후 값 리턴
		}
	}
}
```
``` js title="checkAll - 체크박스 전체 선택/해제"
function checkAll(chkObj, targetId){
	$("input[name=" +targetId+ "]").prop("checked", chkObj.checked);
}
```

