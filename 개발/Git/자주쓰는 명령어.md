---
sticker: emoji//270f-fe0f
---
``` bash title="git으로 github 소스 올리기"
(... 프로젝트 경로 진입 ..)

git init

(... .gitigonre 설정 ..)

git add .
git commit -m "최초 배포"
git remote add origin https://github.com/username/myproject.git
git branch -M main
git push -u origin main

```
``` bash title="강제 업데이트 (Override and Update)"
git reset --hard
git pull
```
``` bash title="github 소스 내려받기"
git clone https://github.com/BBongsu/Obsidian.git
```
``` bash title="github 계정 설정"
git config --global user.email jbsproject500@gmail.com
```
``` bash title="pull 없이 push 해서 발생한 충돌 해결"
git pull origin main --force
git add ETF/시작.md

(.. 충돌한 소스 수정 후 아래 진행 ..)

git commit -m "pull없이 push해서 발생한 충돌 해결 (ETF/시작.md)"
git push origin main

```
``` bash title="Obsidian 모바일 재설치"
# 핸드폰 "내 파일" 들어가서 Obsidian 폴더 삭제

# 패키지 업데이트 및 Git 설치
pkg update && pkg upgrade -y
pkg install git -y

# 스마트폰 파일 접근 권한 요청 (팝업 뜨면 허용 선택)
termux-setup-storage

# 모바일 옵시디언이 접근할 수 있는 경로로 이동
cd ~/storage/shared/Documents

# 내 깃허브의 옵시디언 노트 저장소 복제 (경로는 본인 주소로 변경)
git clone --separate-git-dir="$HOME/obsidian-git/.git" https://github.com/사용자이름/레포지토리이름.git

```