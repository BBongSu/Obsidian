---
sticker: emoji//270f-fe0f
---

``` bash title="강제 업데이트 (Override and Update)"
git reset --hard
git pull
```
``` bash title="github 소스 내려받기"
git clone https://github.com/BBongsu/Obsidian.git
```
``` bash title="github 계정 설정"
git config --global user.email jbsproject500@gmail.com
```
``` bash title="pull 없이 push 해서 발생한 충돌 해결"
git pull origin main --force
git add ETF/시작.md

(.. 충돌한 소스 수정 후 아래 진행 ..)

git commit -m "pull없이 push해서 발생한 충돌 해결 (ETF/시작.md)"
git push origin main

```
